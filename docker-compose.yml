version: '2'

services:
  app:
    build: .
    volumes:
      - /usr/share/nginx/html
  web:
    image: nginx:alpine
    depends_on:
      - app
    ports:
      - "80:80"
    volumes_from:
      - app:ro

# @TODO Mount/copy config for nginx server with php-fpm
